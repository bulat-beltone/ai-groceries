# Local server (npm run dev): copy to .env or export OPENAI_API_KEY=...
OPENAI_API_KEY=

# GitHub Pages: do not put the key in the repo. Use Cloudflare Worker + `wrangler secret put OPENAI_API_KEY` (see openmemory.md).
